# netlify.toml

[build]
  # Kommando som bygger prosjektet
  command = "npm run build"
  
  # Mappen som skal publiseres
  publish = "dist"
  
  # Ignore builds hvis commit-melding ikke inneholder ordet "deploy"
  ignore = "git log -1 --pretty=%B | grep -iq 'deploy' || exit 0"