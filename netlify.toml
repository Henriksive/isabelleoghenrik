# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"
  ignore = """
if git log -1 --pretty=%B | grep -iq deploy; then
  echo 'Deploy trigger found → building'
  exit 1
else
  echo 'No deploy trigger → skipping build'
  exit 0
fi
"""